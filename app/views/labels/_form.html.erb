<%= form_with(model: label, local: true) do |form| %>
  <% if label.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(label.errors.count, "error") %> prohibited this label from being saved:</h2>

      <ul>
      <% label.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="container d-flex justify-content-center">
  <div class="card">
    <div class="card-body">
      <h5 class="card-title">Label</h5>
      <div class="field">
        <%= form.label :label_type %>
        <%= form.text_field :label_type %>
      </div>
      <%= form.submit name: "register", class: "btn btn-primary mt-1" %>
    </div>
  </div>
</div>
<% end %>
